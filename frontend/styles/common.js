import { StyleSheet } from 'react-native';

export const commonStyles = StyleSheet.create({
    container: {
        flex: 1,
        padding: 16,
    },
    normalBackground: {
        backgroundColor: '#f0fbfe',
    },
    errorBackground: {
        backgroundColor: '#ea9188',
    },
});
